# 用户原话逐字要求

本文件保留用户原话。执行任务时必须逐句读取、逐句核对。不要把本文件当概要，不要只看其他改写文件。任何一句适用要求没有落实，都不能声称任务完成。

最新纠正规则：`主图和视频` 是一个文件夹名称，不是分别创建 `主图` 和 `视频` 两个文件夹。`主图和视频` 文件夹里只有多个以产品编号命名的放置图片的文件夹；子文件夹名称是产品编号；子文件夹里面是对应图片。不要额外创建 `主图`、`视频`、`main`、`video` 等分层。

```text
不要涉及到厂家，批发等字眼。

文件的路径在后面提到的路径里。

确定目录位置 (下方的注意点有说) 

整个上架系统的目录位置：

里面有价格表，上架表位于系统文件(勿碰)/上架表文件里 。系统文件(勿碰)/成品图是我放置图片的地方，表示要上哪种产品。 
同一个产品的不同规格是公用一套颜色图的(其实有的sku用的图片用的是一样的，只要能映射出来就可以了，所以没必要大量复制相同的图片)。颜色图的顺序不改变，就跟我原来的一样就行。再帮我单独在主图和视频文件夹里里形成各个文件夹，文件夹的名字是对应的产品编号，文件里面是对应的产品编号对应的图片。图片是用 系统文件(勿碰)成品图 里面的图，是一一对应的，你需要根据我的整个流程进行填写价格表，对应类目的上架表，整理图片zip(放在系统文件(勿碰)\图片zip文件里)，填写 系统文件(勿碰)\上架任务总表.xlsx（按示例的格式填，新填的不要标黄)。

各个产品的卖点和参数来源于产品信息文件夹里的对应产品卖点图文件里的图片里，还有txt文件内容。(是一一对应的)


价格表的填写规则：
接着前面的空白处填，不要覆盖原来的数据。A列序号和D列产品编号接着前面顺序填(不能与旧数据重复)，C，H，J列的值我都在 产品信息文件夹里的对应产品卖点图文件里的txt内容里(注意单位转换)。E列类目填根据产品进行判断着填，H列填的是人民币，我给你的所有的数都是人民币，不要拿卢布算，I是包装重量，注意单位。你需要在对应的u列填写一个数字，利润率和最低利润率等等其他有公式的自动根据公式填好，利润率定为多少看下方注意点。这个和其他的都是根据已经做好的excel公式来自动计算的，你填数字就行了，其他的根据公式算。注意你不能覆盖和隐藏我的公式，要保留，但是填的都是数字，并且价格表的所有数字保留两位小数。AF和AG列填的内容跟之前的旧数据一样，不要不填。序号和库存是整数，不要保留小数。最低价格不能低于售价的 50%，O列的其他费用和前列一致，如果最下方注意点提到了其他费用，则其他费用的填写以注意点的为准。


上架表位于系统文件(勿碰)\上架表里，里面分别是不同类目的表。怎么知道填哪个类目表：看产品信息里的对应产品文件夹里的txt文件，txt里的内容填的类目行有写。比如假设 产品信息\产品1，产品1里有一个txt文件。txt里面类目一行写的是"假饵"   那你需要填的上架表就是"假饵.xlsx"
上架表的通用填写规则：
接着前面的空白处填，不要覆盖原来的数据，1.商品基础信息表 ： A列就是产品编号(不能重复且和价格表的产品编号是一一对应的，并且产品编号的开头第一个数字和旧数据是保持一样的）。
B列和C列填一样的，都是俄语标题。D，E列都填的是俄文描述(简介的内容最好加点小表情，描述不要以“从图片和同类产品的主流卖点来看”的口吻，也不要“这款产品”的人称方式来进行介绍，将人称去掉。比如“在使用体验上兼顾了”前面没有任何人称，注意，你说的简介和标签都是直接放到ozon上的，请注意口吻。各个标题要有区分，不要都一样。要体现每个产品的独特点。还有一定要结合ozon数据和网站分析，不要直接给我。标题，简介不要都一模一样，一定是有区分的。标签一定是能吸引买家的，这点你要搜索ozon信息。认真的分析。 。简介和描述要写6行，内容丰富。F列空着，G和H列都填人民币，分别和价格表的P列(您的价格)，O(折扣前价格)填一样的数字，再强调一遍填的是人民币，填数字就行，价格不用在乎列的名字。I列填0，J列填的是对应价格表的I列毛重，但是注意kg和g的转化，价格表是kg,上架表填的是g,表示的是包装的重量。K，L，M列分别是包装的长宽高，填的是cm,N列和价格表的库存保持一致。O列P列Q列和R列都跟前面旧数据填的一样，不能空着。
2.类目属性表：A列就是产品编号，B列你判断着填如果是下面的注意点里写了，比如“品牌：XXX”，你才能填对应的，否则你就不能填，这个不能通过自己判断得到，必须是我按格式写了，你才能填，否则只能填“Нет бренда”,C列填产品编号，D列跟前面填的一样，E列填主题标签，28个左右( 符合我这个的产品的关键词。俄文版本，俄语的每个标签如果是多个词就用下划线隔开，标签每个前面都要加#，不同的标签之间则用空格隔开，一定要结合ozon数据分析，标题不要带尺寸。所有的内容都不要涉及到颜色。主题标签不能出现 Ozon 敏感品牌词，每个标签的字符不能超过30个），后面有一些是很多属性是多选，像这种多选都是开头列不填，然后一个值你就从起始的下一列开始填，(第一列只是说明)，多个值你就往后填。后面的多选比如颜色都是同理。  attr_9024_卖家代码 也填产品编号，attr_4180_名称 填的也是产品标题，attr_4191_简介 也是填的产品简介，这两个跟前面一样，商品颜色：如果是单品，你自己判断颜色填，数量你自己决定（最多不超过两个），如果是组合品，那颜色的个数只能填组合品的个数。(颜色有一个总规则：填了多色就不能填其他颜色了，只能填一个，但多色尽量少填，可以填多种颜色来替代多色，避免重复) 颜色名称一定要填俄语，中间用逗号隔开，前面的颜色填的是字典值。                                                                                                   
上架表总原则：能多填就多填，我没告诉你的你自己通过分析图品和ozon判一定断也要填上，有字典值的一定要根据字典值填(除了品牌)！！！不能自己捏造。上架表填写一定要注意单位转化。标签和简介都不要包含任何品牌信息和
违禁词，一定要结合ozon数据分析，给我好卖和跟我的产品贴切的，上架表的description_category_id和type_id 和currency_code，要跟表前面填的一样，attr_22232_欧亚经济联盟的HS编码【字典】也要填，也是跟表前面的旧值填的一样。


你还要记住，每次处理完之后，要帮我填好 上架任务总表.xlsx 这个表（不要标黄），按照表原来的格式填，不要超过100个，超过100个就要分两批上，你记住。

还有最重要的标题和标签，尤其是标签，你一定要帮我进行分析和网上分析ozon的相关数据进行分析，一定要给我最好的标签，别人最好搜到的，要结合ozon的数据给我。

所有你本本次任务产生的所有过度文件全部放在 “系统文件(勿碰)\临时文件放置处”里，其他的任何地方都不能产生新文件。


另外不同的类目有单独要注意的规则，具体你去"系统文件(勿碰)\要注意的点"里面看看，是一一对应的

都弄好后自动根据“系统文件(勿碰)\上架任务总表.xlsx”里面最新的一行帮我填好自动上架工具里面的店铺上架的各个路径和行数栏，全部填好。两个表和zip的路径也要自动自动填进去，不要让我填任何东西。还有Ozon task_id按上面的顺序填
任何表都不要改变我的格式。只用我原来的表格式。任何表的长数字直接填，不要用科学计数法，直接文本显示就行。
还有如果下方的注意点和上方的有冲突的话，按下方的注意点来就行。
```
