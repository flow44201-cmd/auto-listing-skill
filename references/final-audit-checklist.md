# 最终验收清单

交付前必须逐项核对。任何一项不通过，不得声称任务完成。

## 文件和目录检查

- 已动态确认当前店铺根目录，未把示例绝对路径当作固定路径。
- 当前店铺根目录包含 `产品信息` 或 `产品信息文件夹`、`系统文件(勿碰)`、`主图和视频`、`价格表.xlsx`、`上架任务总表.xlsx` 等标志项目。
- 如果存在多个候选店铺目录，已根据 `系统文件(勿碰)\成品图` 和店铺目录名称确认当前目录，未跨店铺混用文件。
- 已读取总规则 txt。
- 已读取本次类目对应的 `系统文件(勿碰)\要注意的点`。
- 已读取每个产品对应的 `产品信息` 或 `产品信息文件夹` txt。
- 已检查每个产品对应的卖点图、参数图或详情图。
- 图片形式的卖点图/参数图/详情图已做视觉识别或 OCR，未跳过图片内参数。
- 已检查 `系统文件(勿碰)\成品图` 中的待上架产品。
- 已确认本次上架产品范围以 `系统文件(勿碰)\成品图` 为准，未仅凭产品信息资料额外上架。
- 所有中间文件都只放在 `系统文件(勿碰)\临时文件放置处`。
- 未在其他位置产生过渡文件。

## 产品和编号检查

- 本次产品数量已统计。
- 若超过 100 个，已分批。
- 每个产品都有对应产品信息。
- 每个产品都有对应成品图。
- 每个成品图产品都已和产品信息资料一一对应。
- 每个产品已确定类目。
- 产品编号接旧数据继续。
- 产品编号开头数字已从用户要求、店铺配置、初始化脚本、价格表旧数据或上架表旧数据动态确认。
- 产品编号没有与旧数据重复。
- 价格表和上架表产品编号一一对应。
- 长数字没有科学计数法显示。

## 价格表检查

- 新数据接在旧数据后面空白处。
- 未覆盖旧数据。
- A 列序号连续且为整数。
- D 列产品编号连续且不重复。
- C/H/J 列已按产品 txt 填写并完成单位转换。
- E 列类目判断正确。
- H 列金额按人民币处理，没有按卢布换算。
- I 列包装重量单位为 kg。
- U 列已填利润率/最低利润率相关数字。
- 公式列未被覆盖、删除、隐藏或清空。
- 有公式的列仍由公式计算。
- 所有普通数字保留两位小数。
- 序号和库存为整数。
- 最低价格不低于售价 50%。
- O 列其他费用按旧数据或注意点填写。
- AF/AG 列已按旧数据模式填写，未留空。
- 价格表格式未改变。

## 上架表选择检查

- 已根据每个产品 txt 的“类目”行选择对应类目表。
- 没有把产品填入错误类目表。
- 对应类目的特殊注意点已应用。

## 商品基础信息表检查

- 新数据接在旧数据后面空白处。
- 未覆盖旧数据。
- A 列产品编号与价格表 D 列一致。
- B/C 列俄语标题一致。
- 标题已结合 Ozon 数据和产品卖点分析。
- 不同产品标题有区分。
- 标题不带尺寸。
- 标题不涉及颜色。
- 标题不含敏感品牌词、违禁词、无授权品牌词。
- D/E 列俄文描述一致或符合旧表要求。
- 描述为 6 行，内容丰富。
- 描述没有“从图片来看”“同类产品主流卖点来看”等分析口吻。
- 描述没有“这款产品”等不合适人称。
- 描述适合直接发布 Ozon。
- F 列为空。
- G 列等于价格表 P 列，人民币数字。
- H 列等于价格表 O 列，人民币数字。
- I 列为 0。
- J 列由价格表 I 列 kg 转 g。
- K/L/M 列包装长宽高为 cm。
- N 列库存与价格表一致。
- O/P/Q/R 列按旧数据填写，未留空。
- 表格式未改变。

## 类目属性表检查

- A 列产品编号正确。
- B 列品牌只在用户明确写 `品牌：XXX` 时填品牌，否则为 `Нет бренда`。
- 没有自行判断品牌。
- C 列产品编号正确。
- D 列按旧数据填写。
- E 列主题标签约 28 个。
- 标签为俄文。
- 每个标签前有 `#`。
- 多词标签用下划线。
- 标签之间用空格分隔。
- 标签结合 Ozon 数据和产品卖点。
- 标签不涉及颜色。
- 标签不含敏感品牌词、违禁词、无授权品牌词。
- 每个标签不超过 30 个字符。
- 不同产品标签有区分。
- 多选属性从说明列后一列开始填。
- 多选属性没有错位。
- 有字典值的字段按字典值填写，未捏造。
- `attr_9024_卖家代码` 已填产品编号。
- `attr_4180_名称` 已填产品标题。
- `attr_4191_简介` 已填产品简介。
- `description_category_id` 按旧数据填写。
- `type_id` 按旧数据填写。
- `currency_code` 按旧数据填写。
- `attr_22232_欧亚经济联盟的HS编码【字典】` 按旧数据填写。
- 颜色字段符合单品/组合品规则。
- 填“多色”时没有再填其他颜色。
- 颜色名称为俄语并用逗号分隔。
- 表格式未改变。

## 图片和 zip 检查

- 主图文件夹已按产品编号建立子文件夹。
- 视频文件夹已按产品编号建立子文件夹。
- 主图和视频文件夹位于当前店铺根目录的旧流程位置，未建到其他店铺目录。
- 每个产品编号对应的图片正确。
- 图片来源于 `系统文件(勿碰)\成品图`。
- 同产品不同规格共用颜色图时，未大量重复复制。
- 颜色图顺序未改变。
- 图片 zip 已生成。
- 图片 zip 已放入 `系统文件(勿碰)\图片zip文件`。
- zip 内图片映射与产品编号一致。

## 上架任务总表检查

- 已填写 `系统文件(勿碰)\上架任务总表.xlsx`。
- 按原示例格式填写。
- 新填行没有标黄。
- 没有改变格式。
- 单批不超过 100 个产品。
- 已填写价格表路径。
- 已填写上架表路径。
- 已填写图片 zip 路径。
- 已填写产品编号范围。
- 已填写行数范围。
- 已填写 Ozon task_id 或相关顺序字段。

## 自动上架工具检查

- 已根据上架任务总表最新一行回填自动上架工具。
- 已填价格表路径。
- 已填上架表路径。
- 已填图片 zip 路径。
- 已填主图路径。
- 已填视频路径。
- 已填开始行/结束行/行数栏。
- 已填 Ozon task_id。
- 没有要求用户手动填写路径或行数。

## 最终回复检查

最终回复用户必须包含：

- 本次处理产品数量。
- 产品编号范围。
- 价格表新增行范围。
- 上架表新增行范围。
- 图片 zip 路径。
- 上架任务总表新增行。
- 自动上架工具是否已回填。
- 是否有未完成项。
- 若有未完成项，说明原因和需要用户补充的内容。
