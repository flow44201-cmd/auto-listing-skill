# 最终验收清单

交付前必须逐项核对。任何一项不通过，不得声称任务完成。

必须逐句核对 `references/verbatim-user-requirements.md`。该文件是用户原话逐字版本；任何一句适用要求没有落实，都不得声称任务完成。

同时必须逐项核对 `references/original-requirements-coverage.md`。该文件是用户原始要求的覆盖表，任何适用项目没有完成，都不得声称任务完成。

同时必须逐句核对 `references/full-original-instructions.md`。该文件不是概要，而是逐句完整要求；任何一句适用要求没有落实，都不得声称任务完成。

## 文件和目录检查

- 已用 UTF-8 正确读取 skill 和 references 文件；如果曾出现乱码，已重新用 UTF-8 读取后再执行。
- 已动态确认当前店铺根目录，未把示例绝对路径当作固定路径。
- 如果用户用 `文件在...里/中/下面/目录里/文件夹里/路径里` 描述路径，已剥离句尾位置词后验证路径，未把句尾 `里` 等字误当成文件夹名。
- 已检查用户后面、下方注意点、后续说明或本次消息后半部分提到的路径，并优先使用这些路径。
- 如果用户下方注意点或类目注意点指定了完整目录位置，已优先使用该目录。
- 当前店铺根目录包含 `产品信息` 或 `产品信息文件夹`、`系统文件(勿碰)`、`主图和视频`、`价格表.xlsx`、`上架任务总表.xlsx` 等标志项目。
- 如果用户消息或注意点指定了店铺文件夹名称/目标文件夹名称/图片上一级目录名称，已只选择文件夹名匹配的店铺目录。
- 如果存在多个候选店铺目录，已根据 `系统文件(勿碰)\成品图` 和店铺目录名称确认当前目录，未跨店铺混用文件。
- 如果目标文件夹名称匹配不到或匹配多个目录，已停止并向用户确认，未擅自处理其他店铺。
- 已读取总规则 txt。
- 已读取本次类目对应的 `系统文件(勿碰)\要注意的点`。
- 已读取每个产品对应的 `产品信息` 或 `产品信息文件夹` txt。
- 已检查每个产品对应的卖点图、参数图或详情图。
- 图片形式的卖点图/参数图/详情图已做视觉识别或 OCR，未跳过图片内参数。
- 已检查 `系统文件(勿碰)\成品图` 中的待上架产品。
- 已确认本次上架产品范围以 `系统文件(勿碰)\成品图` 为准，未仅凭产品信息资料额外上架。
- 所有中间文件都只放在 `系统文件(勿碰)\临时文件放置处`。
- 未在其他位置产生过渡文件。

## 产品和编号检查

- 本次产品数量已统计。
- 若超过 100 个，已分批。
- 每个产品都有对应产品信息。
- 每个产品都有对应成品图。
- 每个成品图产品都已和产品信息资料一一对应。
- 每个产品已确定类目。
- 产品信息 txt 新模板中的 `1.详细区` 已读取。
- `国内链接：` 已读取。
- `规格有：` 已读取，并用于规格、SKU、产品编号、价格、颜色和链接数映射核对。
- `对应的价格分别/都是：再加上运费` 已读取并用于价格/成本判断。
- `颜色有     种` 已读取并用于颜色/链接/规格核对。
- `链接数=颜色   种*    =` 已读取并用于链接数、颜色数、规格数核对。
- `2.粘贴区域` 已读取。
- `商品属性：“   ”` 已读取。
- `包装信息：“   ”` 已读取。
- `3.自己重新打包(非自己打包不填)` 已检查；有内容时已使用。
- `包装尺寸分别/都是    *  *  cm` 已读取，长宽高单位为 cm。
- `包装的重量分别商品重量+          g` 已读取，包装重量按商品重量加额外重量计算。
- 新版详细区没有商品重量行时，已从粘贴区域、产品信息、图片、旧表同类数据或合理估算中取得商品重量。
- txt 中已填内容没有被跳过。
- `剩下的你自己估算` 已按图片、产品资料、旧表、类目属性、Ozon 数据和合理规则处理。
- 若粘贴区域和自己重新打包区块冲突，已按自己重新打包区块为准。
- 已完全忽略 `2.粘贴区域` 中的品牌、产地、贸易属性、专利、产品上市时间、主要下游平台、平台名称、主要销售地区、地区名称、有可授权的自有品牌、是否跨境出口专供货源、版权、是否进口等信息。
- 已确认上述忽略信息没有写进标题、简介、描述、标签、主题标签、类目属性自由文本或卖点。
- 类目判断已结合产品信息 txt、成品图、卖点图、类目注意点、旧表和 Ozon 数据。
- txt 有类目行时已读取类目行。
- txt 无类目行或类目不明确时，已自行判断或在高风险时停止确认。
- 不同类目的产品没有写进同一个上架表。
- 每个类目使用对应类目上架表。
- 多类目任务已拆分上架表新增行范围。
- 上架任务总表中不同类目已单独成行。
- 上架任务总表每一行只对应一个类目的上架表。
- 每个类目任务都有对应上架表路径、行数范围、产品编号范围和 Ozon task_id。
- 产品编号接旧数据继续。
- 产品编号开头数字已从用户要求、店铺配置、初始化脚本、价格表旧数据或上架表旧数据动态确认。
- 如果用户本次消息明确写了产品编号开头，例如 `产品编号开头：21`，已按用户本次消息使用并记录来源。
- 未擅自把产品编号开头固定成 `1`、`21` 或其他没有来源依据的前缀。
- 产品编号没有与旧数据重复。
- 价格表和上架表产品编号一一对应。
- 长数字没有科学计数法显示。

## 价格表检查

- 新数据接在旧数据后面空白处。
- 未覆盖旧数据。
- A 列序号连续且为整数。
- D 列产品编号连续且不重复。
- C/H/J 列已按产品 txt 填写并完成单位转换。
- E 列类目判断正确。
- H 列金额按人民币处理，没有按卢布换算。
- I 列包装重量单位为 kg。
- U 列已填利润率/最低利润率相关数字。
- 公式列未被覆盖、删除、隐藏或清空。
- 有公式的列仍由公式计算。
- 所有普通数字保留两位小数。
- 序号和库存为整数。
- 最低价格不低于售价 50%。
- O 列其他费用按旧数据或注意点填写。
- AF/AG 列已按旧数据模式填写，未留空。
- 价格表格式未改变。

## 上架表选择检查

- 已根据每个产品 txt 的“类目”行选择对应类目表。
- 没有把产品填入错误类目表。
- 对应类目的特殊注意点已应用。

## 商品基础信息表检查

- 新数据接在旧数据后面空白处。
- 未覆盖旧数据。
- A 列产品编号与价格表 D 列一致。
- B/C 列俄语标题一致。
- 标题已结合 Ozon 数据和产品卖点分析。
- 不同产品标题有区分。
- 标题不带尺寸。
- 标题不涉及颜色。
- 标题不含敏感品牌词、违禁词、无授权品牌词。
- 标题不涉及“厂家”“批发”“产地”“年份”等字眼，也不含对应俄文/英文含义的厂家、工厂、批发、批发价、供应商、源头厂家、产地、原产地、生产年份、年份等表达。
- D/E 列俄文描述一致或符合旧表要求。
- 描述为 6 行，内容丰富。
- 描述没有“从图片来看”“同类产品主流卖点来看”等分析口吻。
- 描述没有“这款产品”等不合适人称。
- 描述适合直接发布 Ozon。
- 描述/简介不涉及“厂家”“批发”“产地”“年份”等字眼，也不含对应俄文/英文含义的厂家、工厂、批发、批发价、供应商、源头厂家、产地、原产地、生产年份、年份等表达。
- F 列为空。
- G 列等于价格表 P 列，人民币数字。
- H 列等于价格表 O 列，人民币数字。
- I 列为 0。
- J 列由价格表 I 列 kg 转 g。
- K/L/M 列包装长宽高为 cm。
- N 列库存与价格表一致。
- O/P/Q/R 列按旧数据填写，未留空。
- 表格式未改变。

## 类目属性表检查

- A 列产品编号正确。
- B 列品牌只在用户明确写 `品牌：XXX` 时填品牌，否则为 `Нет бренда`。
- 没有自行判断品牌。
- C 列产品编号正确。
- D 列按旧数据填写。
- E 列主题标签约 28 个。
- 标签为俄文。
- 每个标签前有 `#`。
- 多词标签用下划线。
- 标签之间用空格分隔。
- 标签结合 Ozon 数据和产品卖点。
- 标签不涉及颜色。
- 标签不含敏感品牌词、违禁词、无授权品牌词。
- 标签不涉及“厂家”“批发”“产地”“年份”等字眼，也不含对应俄文/英文含义的厂家、工厂、批发、批发价、供应商、源头厂家、产地、原产地、生产年份、年份等表达。
- 即使用户给的资料里出现厂家、批发、产地、年份或厂家信息，也已忽略，没有写进标题、简介、描述、标签或属性文本。
- 已确认页面表达重点是产品信息，不提厂家信息。
- 每个标签不超过 30 个字符。
- 不同产品标签有区分。
- 已执行“能多填就多填”：能通过图片、卖点图、txt、旧表同类数据、类目属性含义、Ozon 数据或注意点合理判断的属性，已尽量填写，没有无理由留空。
- 对每个未填写的可选属性，已确认无法从图片、卖点图、txt、旧表同类数据、类目属性含义、Ozon 数据或注意点可靠判断，或该字段不适用。
- 多选属性从说明列后一列开始填。
- 多选属性没有错位。
- 有字典值、下拉值、枚举值或旧表已有字典值模式的字段，已按表内字典值/旧表字典值填写。
- 没有自己捏造字典值，没有随意翻译成非字典值，没有用自由文本替代字典值。
- `attr_9024_卖家代码` 已填产品编号。
- `attr_4180_名称` 已填产品标题。
- `attr_4191_简介` 已填产品简介。
- `description_category_id` 按旧数据填写。
- `type_id` 按旧数据填写。
- `currency_code` 按旧数据填写。
- `attr_22232_欧亚经济联盟的HS编码【字典】` 按旧数据填写。
- 颜色字段符合单品/组合品规则。
- 填“多色”时没有再填其他颜色。
- 颜色名称为俄语并用逗号分隔。
- 表格式未改变。

## 防 Ozon 误合并检查

- 已检查同一批商品没有因为过度相似而容易被 Ozon 误判为重复商品。
- 已检查不会因为重复度太高导致 Ozon 强行合并商品卡片。
- 已检查不会因为重复度太高导致 `无法与其他商品合并 / 至少更改一个可变特性`。
- 已检查同一批商品的类目没有不合理地过度相似。
- 已检查同一批商品的标题没有不合理地过度相似。
- 已检查同一批商品的型号没有不合理地过度相似。
- 已检查同一批商品的组合字段没有不合理地过度相似。
- 已检查同一批商品的颜色没有不合理地过度相似。
- 已检查同一批商品的重量没有不合理地过度相似。
- 已检查同一批商品的长度没有不合理地过度相似。
- 已检查同一批商品的图片结构没有不合理地过度相似。
- 已检查同一批商品的规格描述没有不合理地过度相似。
- 每个商品都有明确差异点。
- 对不需要合并成同一卡片的商品，`型号名称（针对合并为一张商品卡片）` 已按 offer_id 或真实规格生成唯一值。
- 对不需要合并成同一卡片的商品，`组合成类似的产品` 已按 offer_id 或真实规格生成唯一值。
- 对不需要合并成同一卡片的商品，`颜色名称` 已按 offer_id 或真实规格生成唯一值或真实差异表达。
- 对不需要合并成同一卡片的商品，`卖家代码` 已按 offer_id 或真实规格生成唯一值。
- 如适用，已使用类似 `model-{offer_id}` 的方式区分型号名称。
- 如适用，已使用类似 `series-{offer_id}` 的方式区分组合字段。
- 未整批商品使用完全相同的型号名称。
- 未整批商品使用完全相同的组合字段。
- 未整批商品使用完全相同的颜色名称。
- 未整批商品使用完全相同的标题结构。
- 未整批商品使用高度重复的描述。
- 标题、颜色名、规格、重量、尺寸、用途、适用场景等字段已尽量体现真实差异。
- 差异来自真实产品、真实规格、真实图片、真实重量、真实尺寸、真实用途或合理的 Ozon 字段区分，没有为了去重虚构错误参数。

## 标题和属性分析检查

- 生成标题和属性前，已先理解当前产品本身。
- 已根据商品图片分析产品。
- 已根据 1688 链接或其他来源链接分析产品。
- 已根据产品名称分析产品。
- 已根据类目分析产品。
- 已根据规格信息分析产品。
- 已根据竞品信息进行搜索和分析。
- 已判断产品在 Ozon 上适合的标题结构。
- 已判断产品在 Ozon 上适合的核心关键词。
- 已判断产品在 Ozon 上适合的规格表达。
- 已判断产品在 Ozon 上适合的关键属性。
- 没有随便给标题。
- 没有只翻译中文名。
- 没有直接套固定模板。
- 标题符合该类产品在 Ozon/RU 市场里的真实搜索习惯。
- 标题突出买家会搜索的核心词、材质、用途、规格、适用场景和关键卖点。
- 属性填写服务于标题、类目匹配和搜索曝光。
- 每个标题和属性都结合产品实际分析后生成。
- 标题和属性没有总结式、笼统式、模板式处理。
- 没有把用户要求概括成一句“优化标题”或“优化属性”后省略细节。

## 图片和 zip 检查

- `主图和视频` 是一个文件夹名称，未拆成 `主图` 和 `视频` 两个文件夹。
- `主图和视频` 文件夹已按产品编号直接建立子文件夹。
- `主图和视频` 文件夹里只有产品编号命名的文件夹，没有额外创建 `主图`、`视频`、`main`、`video` 等分层。
- 每个产品编号文件夹里放的是对应产品编号的图片。
- `主图和视频` 文件夹位于当前店铺根目录的旧流程位置，未建到其他店铺目录。
- 每个产品编号对应的图片正确。
- 图片来源于 `系统文件(勿碰)\成品图`。
- 同产品不同规格共用颜色图时，未大量重复复制。
- 不同规格共用同一套图片时，已确认无需为每个规格重复复制完全相同的图片。
- 如使用 zip 命名或图片名简化压缩包，已确认 zip 名称和图片名称能映射规格、SKU、产品编号和图片。
- 每个规格都能映射到正确图片。
- 每个 SKU 都能映射到正确图片。
- 每个产品编号都能映射到正确图片。
- 简化后的 zip 与逐规格复制图片时的上架效果一致。
- 未因简化压缩包导致图片缺失、错位、串规格、串 SKU 或串产品编号。
- 如果无法确认自动上架工具能识别简化命名方式，已按旧流程保证功能可用。
- 颜色图顺序未改变。
- 图片 zip 已生成。
- 图片 zip 已放入 `系统文件(勿碰)\图片zip文件`。
- zip 内图片映射与产品编号一致。

## 上架任务总表检查

- 已填写 `系统文件(勿碰)\上架任务总表.xlsx`。
- 按原示例格式填写。
- 新填行没有标黄。
- 如果复制了旧行/示例行格式，已确认新增行没有继承黄色填充。
- 没有改变格式。
- 单批不超过 100 个产品。
- 已填写价格表路径。
- 已填写上架表路径。
- 已填写图片 zip 路径。
- 已填写产品编号范围。
- 已填写行数范围。
- 已填写 Ozon task_id 或相关顺序字段。

## 自动上架工具检查

- 已根据上架任务总表最新一行回填自动上架工具。
- 已填价格表路径。
- 已填上架表路径。
- 已填图片 zip 路径。
- 已填 `主图和视频` 文件夹路径或自动上架工具要求的对应图片路径。
- 没有把 `主图和视频` 路径拆成不存在的 `主图`、`视频` 两个图片目录。
- 已填开始行/结束行/行数栏。
- 已填 Ozon task_id。
- 没有要求用户手动填写路径或行数。

## 最终回复检查

最终回复用户必须包含：

- 本次处理产品数量。
- 产品编号范围。
- 价格表新增行范围。
- 上架表新增行范围。
- 图片 zip 路径。
- 上架任务总表新增行。
- 自动上架工具是否已回填。
- `original-requirements-coverage.md` 和 `final-audit-checklist.md` 是否已逐项核对。
- `full-original-instructions.md` 是否已逐句核对。
- `verbatim-user-requirements.md` 是否已逐句核对。
- 是否有未完成项。
- 若有未完成项，说明原因和需要用户补充的内容。
